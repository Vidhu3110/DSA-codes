/*
    int heightTree(Node *root){
    if(root == NULL)
        return 0;
    else
        return max(heightTree(root->left),heightTree(root->right))+1;
}


printing k+1th element in binary tree

void printK(Node *root , int k){
if(root==NULL)
    return;
if(k==0){
cout<<root->key<<" ";
}
else{
    printk(root->left,k-1);
    printk(root->right,k-1);
}
}
*/
